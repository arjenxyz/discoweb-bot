{
  "name": "disc---web-connec",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run bot\" \"npm run web\"",
    "bot": "cd src/bot && node index.js",
    "web": "cd src/web && npm run dev",
    "setup": "cd src/bot && npm install && cd ../web && npm install",
    "vercel-build": "cd src/web && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
